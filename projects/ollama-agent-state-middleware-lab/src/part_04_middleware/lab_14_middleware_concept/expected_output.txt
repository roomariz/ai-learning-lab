14 Middleware Concept
=====================

Without middleware
==================

RESULT:
note added: middleware runs around agent work

NOTES:
['middleware runs around agent work']

With middleware
===============
[middleware before] about to run: add_note
[middleware after] finished running: add_note

RESULT:
note added: middleware can observe what happens

NOTES:
['middleware runs around agent work', 'middleware can observe what happens']

Conclusion
==========

Middleware is code that runs around the main work. It can observe what happens before and after an action. Later labs use real LangGraph middleware.